# Runtime dependencies. Install the project itself for the full set:
#     pip install -e ".[ml,geo,api,dashboard,dev]"
#
# Pinned minimums are declared in pyproject.toml; this file mirrors the
# core + ml + api extras for environments that cannot use PEP 621 extras.

pandas>=2.2
numpy>=1.26
scipy>=1.13
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

scikit-learn>=1.5
xgboost>=2.1
shap>=0.46
joblib>=1.4
matplotlib>=3.9
seaborn>=0.13
plotly>=5.22

fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
python-multipart>=0.0.9
