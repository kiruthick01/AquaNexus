# Copy to .env.local and adjust. Vite only exposes variables prefixed VITE_.
#
# Substituted at BUILD time, not read at runtime: a container serving the built
# bundle cannot be repointed with an environment variable, it has to be rebuilt
# with --build-arg VITE_API_BASE_URL=...
VITE_API_BASE_URL=http://localhost:8000
